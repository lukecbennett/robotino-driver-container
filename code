# Install rosdep and supporting tools
sudo apt update
sudo apt -y install python3-rosdep python3-rosinstall-generator python3-vcstool build-essential

# Initialize rosdep (only once per machine)
sudo rosdep init
rosdep update
